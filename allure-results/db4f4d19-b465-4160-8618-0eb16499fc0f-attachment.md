# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: alerts.spec.ts >> Alerts >> Promt alert
- Location: tests\alerts.spec.ts:28:14

# Error details

```
Error: expect(locator).toHaveText(expected) failed

Locator:  locator('#confirmResult')
Expected: "You entered skillmea"
Received: "You selected Ok"
Timeout:  5000ms

Call log:
  - Expect "toHaveText" with timeout 5000ms
  - waiting for locator('#confirmResult')
    14 × locator resolved to <span id="confirmResult" class="text-success">You selected Ok</span>
       - unexpected value "You selected Ok"

```

```yaml
- text: You selected Ok
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | 
  4  | test.describe('Alerts', () => {
  5  |     // beforeEach pred kazdym volanim zavola loginPage
  6  |     test.beforeEach(async ({ page }) => {
  7  |         await page.goto('https://demoqa.com/alerts');
  8  |     })
  9  | 
  10 | 
  11 |     test.only('Simple alert', async ({ page }) => {
  12 |         // listener ktory caka pri dialogovych oknach je to dobre
  13 |         page.on("dialog", async dialog => {
  14 |             await dialog.accept();
  15 |         })
  16 |         await page.locator('#alertButton').click();
  17 |     });
  18 | 
  19 |     test.only('Confirm alert', async ({ page }) => {
  20 |         // listener ktory caka pri dialogovych oknach je to dobre
  21 |         page.on("dialog", async dialog => {
  22 |             await dialog.dismiss();
  23 |         })
  24 |         await page.locator('#confirmButton').click();
  25 |         await expect(page.locator('#confirmResult')).toHaveText('You selected Cancel');
  26 |     });
  27 | 
  28 |         test.only('Promt alert', async ({ page }) => {
  29 |         // listener ktory caka pri dialogovych oknach je to dobre
  30 |         page.on("dialog", async dialog => {
  31 |             await dialog.accept('skillmea');
  32 |         })
  33 |         await page.locator('#confirmButton').click();
> 34 |         await expect(page.locator('#confirmResult')).toHaveText('You entered skillmea');
     |                                                      ^ Error: expect(locator).toHaveText(expected) failed
  35 |     });
  36 | })
  37 | 
  38 | 
  39 | 
  40 | 
  41 | 
```