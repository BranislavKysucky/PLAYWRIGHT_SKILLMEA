# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: assertions.spec.ts >> Assertions
- Location: tests\assertions.spec.ts:4:6

# Error details

```
Error: expect(locator).not.toBeEditable() failed

Locator:  locator('#password')
Expected: not editable
Received: editable
Timeout:  5000ms

Call log:
  - Expect "soft not toBeEditable" with timeout 5000ms
  - waiting for locator('#password')
    13 × locator resolved to <input value="" id="password" type="password" name="password" autocorrect="off" data-test="password" autocapitalize="none" placeholder="Password" class="input_error form_input"/>
       - unexpected value "editable"

```

```yaml
- textbox "Password"
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | // Assertions 
  4  | test.only('Assertions', async ({ page }) => {
  5  |     await page.goto('https://www.saucedemo.com');
  6  |     await expect(page.locator('#user-name')).toBeVisible();
  7  |     await expect(page.locator('#password')).toBeEditable();
  8  |     // soft vecicka ze ak test zlyha aj tak to bude pokracovat dalej
> 9  |     await expect.soft(page.locator('#password')).not.toBeEditable();
     |                                                      ^ Error: expect(locator).not.toBeEditable() failed
  10 |     await expect(page.locator('#login-button')).toHaveCount(1);
  11 |     await expect(page.locator('#skillmea')).not.toBeVisible();
  12 | 
  13 |     //toBe, toHave, toContain tiez mozeme pouzit
  14 | });
  15 | 
```